# Exclude backend source and DB migrations (not needed for Vercel preview)
src/backend/
database/

# Python preview server (replaced by Vercel serverless functions)
preview_server.py

# Dev tooling
node_modules/
.env
.claude/
MEMORY.md

# Backend package.json has SQL Server deps that don't exist on npm (connect-mssql-v2).
# The stub API has zero requires — exclude it so Vercel doesn't try to install anything.
package.json
package-lock.json
