{
  "version": 2,
  "buildCommand": "echo 'No build step required'",
  "installCommand": "echo 'No install required — stub API has zero external deps'",
  "outputDirectory": "src/frontend",
  "rewrites": [
    { "source": "/login",  "destination": "/login.html"  },
    { "source": "/logout", "destination": "/api/v1/auth/logout" }
  ]
}
